# docs

Start with architecture, getting-started, api-reference, testing-strategy, requirements-coverage and validation. `decisions/` records alternatives and tradeoffs. The original prompt is at the repository root.
