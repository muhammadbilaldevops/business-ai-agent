# workflows

CI validates Python, the frontend, browser workflows, and container startup in separate jobs. Dependency auditing has its own workflow. Inspect the actual run status before claiming a gate passed.
