# scripts

Operator commands: build static UI, seed synthetic files, explicitly bootstrap models, and run evaluation. Scripts are not tools exposed to the agent. The build compatibility module changes only unavailable process-memory metrics in restricted build environments.
