# security

Adversarial filenames, SQL/table-function attempts, unknown tools, Office expansion limits, untrusted document instructions, auth/origin checks, and an extractive network block. Run `pytest tests/security`.
