# e2e

Playwright exercises user-visible local workflows and axe accessibility checks. Configure and run from `apps/web`. Tests start FastAPI serving the built `dist/` export and isolate browser storage for each test.
