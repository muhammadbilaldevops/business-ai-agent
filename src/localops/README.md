# localops

Core composition: `services.py` creates storage, retrieval, tools, graph and voice. `config.py` reads local settings; `store.py` owns SQLite; `analytics.py` validates and isolates computation.
